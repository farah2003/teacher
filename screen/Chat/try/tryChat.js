import React, { Component } from 'react';
import { View, Text } from 'react-native';

class tryChat extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> tryChat </Text>
      </View>
    );
  }
}

export default tryChat;
